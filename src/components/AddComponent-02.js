function AddComponent02(props) {
    return (
        <h1>
            {props.firstNumber} + {props.secondNumber} = {props.firstNumber + props.secondNumber}
        </h1>
    ); 
}

export default AddComponent02;